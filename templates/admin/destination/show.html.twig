{% extends 'base.html.twig' %}

{% block title %}Destination{% endblock %}

{% block body %}
    <h1>Destination</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ville</th>
                <td>{{ destination.ville }}</td>
            </tr>
            <tr>
                <th>Lat</th>
                <td>{{ destination.lat }}</td>
            </tr>
            <tr>
                <th>Lng</th>
                <td>{{ destination.lng }}</td>
            </tr>
            {# <tr>
                <th>Id</th>
                <td>{{ destination.id }}</td>
            </tr> #}
                <th>Pays</th>
                <td>{{ destination.pays.nom }}</td>
            </tr>
            <tr>
                <th>Created</th>
                <td>{{ destination.created ? destination.created|date('d-m-Y') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_destination_index') }}">Retour à la liste de destinations</a>

    <a href="{{ path('admin_destination_edit', {'id': destination.id}) }}">Modifier une destination</a>

    {{ include('admin/destination/_delete_form.html.twig') }}
{% endblock %}
